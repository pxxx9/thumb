spring:
  data:
    # Redis ??
    redis:
      database: 0
      host: 127.0.0.1
      timeout: 5000
      port: 6379

  # DataSource Config
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/thumb_db
    username: root
    password: 1234
  application.name: thumb
  mvc:
    servlet:
      path: /api
knife4j:
  enable: true
  setting:
    language: zh_cn
# mybatis-plus
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
